{
  "name": "khayal",
  "version": "1.0.0",
  "private": true,
  "description": "خَيال · منصة برومبتات الصور العربية",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd server && npm install --no-audit --no-fund",
    "start": "cd server && node index.js",
    "db:seed": "cd server && node db/setup.js",
    "db:setup": "cd server && node db/setup.js"
  }
}